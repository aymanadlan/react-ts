import Button from "./components/Button/Button";

function app() {
  return (
    <div>
      <Button color="primary" onClick={() => {}}>
        My Button
      </Button>
    </div>
  );
}

export default app;
